</div>
</div>
</div>
<footer>
    <div class="footer" id="footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-4  col-md-4 col-sm-4 col-xs-6">
                    <h3>Get to Know Us</h3>
                    <ul>
                        <li> <a href="<?php echo site_url("genesis"); ?>">Genesis </a> </li>
                        <li> <a href="<?php echo site_url("OurTeam"); ?>">Our Team</a> </li>
                        <li> <a href="<?php echo site_url("CareerWithUs"); ?>">Careers</a> </li>
                        <li> <a href="<?php echo site_url("pressRelease"); ?>">Press Release</a> </li>
                        <li> <a href="<?php echo site_url("ihuntBestCares"); ?>">I HUNTBest Cares</a> </li>
                        <li> <a href="<?php echo site_url("giftSmile"); ?>">Gift a Smile</a> </li>
                        <li> <a href="<?php echo site_url("blog"); ?>">Blog</a> </li>
                    </ul>
                </div>
                <div class="col-lg-4  col-md-4 col-sm-4 col-xs-6">
                    <h3>COLLEGES </h3>
                    <ul>
                        <li> <a href="<?php echo site_url("services"); ?>">Services</a> </li>
                        <li> <a href="<?php echo site_url("testimonial"); ?>">Testimonials</a> </li>
                        <li> <a href="<?php echo site_url("ihuntBestStories"); ?>">I hunt best stories </a> </li>
                        <li> <a href="<?php echo site_url("support"); ?>">Support</a> </li>

                        <li> <a href="<?php echo site_url("paymentsSaved"); ?>">Payments Saved </a> </li>
                        <li> <a href="<?php echo site_url("cardsShipping"); ?>">Cards Shipping</a> </li>
                        <li> <a href="<?php echo site_url("cancelAndReturn"); ?>">Cancellation & Returns</a> </li>
                        <li> <a href="<?php echo site_url('faq'); ?>">FAQ</a> </li>
                        <li> <a href="<?php echo site_url('reportInfringement'); ?>">Report Infringement</a> </li>
                        <li> <a href="<?php echo site_url('writeToUs'); ?>">Write to Us</a> </li>
                    </ul>
                </div>
                <div class="col-lg-4  col-md-4 col-sm-4 col-xs-6">
                    <h3> OUR GROUP</h3>
                    <ul>

                        <li> <a href="<?php echo site_url('showBrandEmpowerment'); ?>">BRAND EMPOWERMENT</a> </li>
                        <li> <a  target="_blank" href="<?php echo site_url('AdvertiseWithUs'); ?>">Advertisement</a> </li>
                        <li> <a href="<?php echo site_url('onlineShopping'); ?>"> Online Shopping</a> </li>
                        <li> <a href="<?php echo site_url('affiliateProgram'); ?>">Affiliate Program</a> </li>
                        <li> <a href="<?php echo site_url('giftCardOffer'); ?>">Gift Card/offer</a> </li>
                        <li> <a href="<?php echo site_url('firstSubscription'); ?>">First Subscription</a> </li>
                        <li> <a href="<?php echo site_url('siteMap'); ?>">Sitemap</a> </li>

                    </ul>
                </div>

                <div class="pay">
                    <h2>PAYMENT METHOD</h2> <ul>
                        <li><a href="#"><i class="fa fa-cc-mastercard" aria-hidden="true"></i></a></li>
                        <li><a href="#"><i class="fa fa-cc-paypal" aria-hidden="true"></i></a></li>
                        <li><a href="#"><i class="fa fa-cc-visa" aria-hidden="true"></i></a></li>
                        <li><a href="#"><i class="fa fa-money" aria-hidden="true"></i></a></li>
                    </ul>
                </div>
            </div>
            <!--/.row-->
            <!--------------------------------------flipkart---------------------------------->
            <div class="row">
                <div class="bo-col-12">
                    <div class="col-md-4" style="padding: 6px;border-right: 1px solid #ccc;">
                        <div class="track-to">
                            <span class="map-to"><i class="fa fa-map-marker" aria-hidden="true"></i></span><h3>Track your<br>Admission</h3>
                        </div>
                    </div>
                    <div class="col-md-4" style="padding: 6px;border-right: 1px solid #ccc;">
                        <div class="track-to">
                            <span class="map-to"><i class="fa fa-refresh" aria-hidden="true"></i></span><h3>Refund Policy</h3>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="track-to">
                            <span class="map-to"><i class="fa fa-times" aria-hidden="true"></i></span><h3>Online Application <br>Submission</h3>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="bo-col-12">
                    <div class="policy">
                        <h5>Policies :</h5>
                        <ul>
                            <li><a href="<?php echo site_url('returnPolicy'); ?>">Returns Policy</a></li>
                            <li><a href="<?php echo site_url('termsOfUse'); ?>">Terms of use</a></li>
                            <li><a href="<?php echo site_url('securityPolicy'); ?>">Security Privacy</a></li>
                            <li><a href="<?php echo site_url('reportInfringement'); ?>">Infringement</a></li>
                            <li><a href="<?php echo site_url('privacypolicy'); ?>">Privacy Policy</a></li>
                        </ul>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="bo-col-12">
                    <div class="col-md-6 policy-to">
                        <div class="policy">
                            <h5 style=" margin-bottom:10px;">Mail Us:</h5>
                            <p> Ozone Manay Tech Park, </p>
                            <p> #**/** &amp; **/**, *th Floor, </p>
                            <p> Garvebhavipalya, Hosur Road, </p>
                            <p> Bangalore - *******, </p>
                            <p> Karnataka, India.</p>
                        </div>
                    </div>
                    <div class="col-md-6 policy-to" style="border-left:1px solid #ccc; padding-left:15px;">
                        <div class="policy">

                            <h5 style=" margin-bottom:10px;">Registered Office Address:</h5>
                            <p> iHuntBest </p>
                            <p> Vaishnavi Summit, Ground Floor, 7th Main, </p>
                            <p> 80 Feet Road, 3rd Block,</p>
                            <p> Bangalore - *******, </p>
                            <p> Karnataka, India.</p>
                            <p>Telephone: 1800 *** 9898</p>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="bo-col-12">
                    <div class="policy">
                        <h5 style=" margin-bottom:10px;">Online college in India at its best</h5>
                        <p class="online">It's no longer just the privilege of a metro city or an urban area to shop online for their favorite products.
                            Flipkart is one online shopping site that has made it possible for consumers even in the remote areas of India to
                            avail products from the best brands at low prices online. Considering the present lifestyle of people, it's no surprise
                            that they prefer to buy online most of the products that they need on a daily basis like clothes for men and women, electronics,
                            mobiles, home appliances, products for personal beauty and care , and the like. The ultimate convenience of having to simply browse
                            through their favorite online shopping website and place orders from the comfort of their home, and get it delivered in the shortest
                            time possible at their doorstep is a service that is unbeatable.</p>


                    </div>
                </div>
            </div>
            <!--------------------------------------flipkart---------------------------------->
        </div>
        <!--/.container-->
    </div>
    <!--/.footer-->

    <div class="footer-bottom">
        <div class="container">
            <div class="col-md-4 social-me-to">
                <p class="pull-left">© <?php echo date('Y'); ?> iHuntBest  </p>
            </div>
            <div class="col-md-4 social-me-to">
                <div class="social">
                    <ul>
                        <li><a href="#"> <i class="fa fa-facebook"></i></a></li>
                        <li><a href="#"> <i class="fa fa-twitter"></i></a></li>
                        <li><a href="#"> <i class="fa fa-google-plus"></i></a></li>
                        <li><a href="#"> <i class="fa fa-instagram"></i></a></li>
                        <li><a href="#"> <i class="fa fa-times-circle"></i></a></li>
                    </ul>
                </div>
            </div>
            <div class="col-md-4 social-me-to">
                <p style="padding-top: 18px;
                   float: right; color:#fff">Powered By: <a href="<?php echo site_url(); ?>" target="_blank">NEDC</a></p>
            </div>
        </div>
    </div>
    <!--/.footer-bottom-->
</footer>
<script src='https://www.google.com/recaptcha/api.js?onload=onloadCallback&render=explicit'></script>

<script type="text/javascript">
	function topBanners(setData) {
		let data_length = setData.length;
		let item_length = $(".item").length;
		if(data_length>0){
			let append_data = '';
			for(let i=0; i<data_length;i++){
				item_length += 1;
				append_data =	append_data+'<div class="item slide'+(item_length)+'">' +
						'<img src="'+setData[i].banner_src+'">' +
						'<div class="row">' +
						'<div class="container">' +
						'<div class="col-md-7 text-left">' +
						'<h5 data-animation="animated bounceInDown">'+setData[i].title+'</h5>' +
						'<p></p>' +
						'<div class="click-search">' +
						'<a target="_blank" ' +
						'href="<?php echo site_url('home/banner_link?link='); ?>'+setData[i].target_url+'&identifier='+setData[i].identifier+'">Click to view more</a>' +
						'</div>' +
						'</div>' +
						'<div class="col-md-5 text-right"></div>' +
						'</div>' +
						'</div>' +
						'</div>';
			}
			$(".carousel-inner").append(append_data);

		}
	}
	getData();


	function getData() {
		$.ajax({
			url: '<?php echo site_url('home/getPosterData'); ?>',
			success: function (response) {

				if(response.status){
					var top_data = [];
					var middle_data = [];
					var right_side = [];
					var left_side = [];
					var total_banners = response.data.length;
					var banners = response.data;

					 for(var i=0;i<total_banners;i++){

						 var process = {banner_src: banners[i].banner,title:banners[i].title,
							 target_url:banners[i].target_url,identifier:banners[i].identifier};
						if(banners[i].page_location=="Top"){
							top_data.push(process);
						}else if(banners[i].page_location=="Middle"){
							middle_data.push(process);
						}else if(banners[i].page_location=="Right Side"){
							right_side.push(process);
						}else if(banners[i].page_location=="Left Side"){
							right_side.push(process);
						}else{

						}
					}
					topBanners(top_data);
				}
			},
			error: function (jqXHR, exception) {
				$.alert({
					title: 'Error!', content: jqXHR["status"] + " - " + exception, type: 'red',
					typeAnimated: true,
					buttons: {
						Ok: function () {

						}
					}
				});
			}
		});
	}
</script>
<script>
    getReligion("", "religion_id");
    function getReligion(selval, id) {
        let result = '<option value="">Select</option>';
        $.ajax({
            url: '<?php echo site_url('home/religion'); ?>',
            success: function (response) {
                if (response !== "") {
                    var respjson = $.parseJSON(response);
                    for (var i = 0; i < respjson.length; i++) {
                        result = result + '<option value="' + respjson[i].religionId + '">' + respjson[i].religionName + '</option>';
                    }
                    $('#' + id).html(result);
                    $('#' + id).val(selval);
                } else {
                    $.alert({
                        title: 'Error!', content: response, type: 'red',
                        typeAnimated: true,
                        buttons: {
                            Ok: function () {

                            }
                        }
                    });
                }
            },
			error: function (jqXHR, exception) {
                $.alert({
                    title: 'Error!', content: jqXHR["status"] + " - " + exception, type: 'red',
                    typeAnimated: true,
                    buttons: {
                        Ok: function () {

                        }
                    }
                });
            }
        });
    }
    function enquiryNow(id, type, courseId, orgCourseId)
    {
        $("#enqorgCourseId").val(orgCourseId);
        $("#enqcourseId").val(courseId);
        $("#enqorgTypeId").val(type);
        if (id !== "" && type !== "" && courseId !== "")
        {
            $.ajax({
                type: "POST",
                data: {id: id, type: type, courseId: courseId, orgCourseId: orgCourseId, reqType: "Enqiry"},
                url: "<?php echo site_url('Home/getEnrollData'); ?>",
                success: function (response) {
                    var json = $.parseJSON(response);
                    if (json.orgDetails !== '')
                    {
                        $("#en_collegelogoenq").prop('src', '<?php echo base_url(); ?>' + json.orgDetails[0].orgLogo);
                        $("#enq_orgname").text(json.orgDetails[0].orgName);
                        $("#enq_orgaddress").text(json.orgDetails[0].orgAddress);

                    }
                    $("#enquiryModal").show();
                    $("body").css("overflow", "hidden");
                    $("#courseDetailsenq").html('<h4 class="enrollnowlabels"  style=" margin-bottom:10px;">Course Details Enquiring For</h4>');
                    $("#courseDetailsenq").append(json.CourseDetails);

                }, error: function (jqXHR, exception) {
                  $(".tablink-01").trigger('click');
                        $("#myModal").css("display", "block");
                        return false;
                }
            });
        }
    }
    function enrollnow(id, type, courseId, orgCourseId)
    {
		// var chk_enroll_status= $("#chk_enroll").val();
		// if(chk_enroll_status==1)
		// {
			// alert('You are already enrolled for this course');
			// $("#chk_enroll").val('');
			// window.location.href = "<?php echo site_url('Home/enrollset'); ?>";
		// }
		// else{
        $("#submitbtn").html("");
        if (id !== "" && type !== "" && courseId !== "")
        {
            $("#orgCourseId").val(orgCourseId);
            $("#enrcourseId").val(courseId);
            $("#orgTypeId").val(type);
			$("#lgId").val(id);
            $.ajax({
                type: "POST",
                data: {id: id, type: type, courseId: courseId, orgCourseId: orgCourseId},
                url: "<?php echo site_url('Home/getEnrollData'); ?>",
                success: function (response) {
                    var json = $.parseJSON(response);
                    if (json !== 'login' && json.studentDetails !== "")
                    {
                        var baseurl = "<?php echo site_url(); ?>";
                        $("#studentImageenroll").prop('src', baseurl + json.studentDetails.studentImage);
                        $("#en_name_id").val(json.studentDetails.studentName);
                        $("#en_birthday_id").val(json.studentDetails.dob);
                        $("#en_mobile_id").val(json.studentDetails.studentMobile);
                        $("#en_father_id").val(json.studentDetails.fatherName);
                        $("#en_placeofb_id").val(json.studentDetails.placeofBirth);
                        $("#religion_id").val(json.studentDetails.religion);
                        $("#en_nationality_id").val(json.studentDetails.name);
                        $("#en_email_id").val(json.studentDetails.email);
                    } else {
                        $(".tablink-01").trigger('click');
                        $("#myModal").css("display", "block");
                        return false;
                    }
                    if (json.CourseDetails !== "") {
                        $("#courseDetails").html('<h4 class="enrollnowlabels"  style=" margin-bottom:10px;">Course Details Applying For</h4>');
                        $("#courseDetails").append(json.CourseDetails);
                    }
                    if (json.agePreReqTable) {
                        $("#agetablediv").html(json.agePreReqTable);
                    }
                    if (json.isAgeEligible === "Yes") {
                        $("#iseligibleage").removeClass("hidden");
                        $("#isnoteligibleage").addClass("hidden");
                        $("#agePreReqDiv").addClass("greenborder");
                        $("#agePreReqDiv").removeClass("redborder");
                    } else {
                        $("#isnoteligibleage").removeClass("hidden");
                        $("#iseligibleage").addClass("hidden");
                        $("#agePreReqDiv").addClass("redborder");
                        $("#agePreReqDiv").removeClass("greenborder");
                    }
                    $("#requiredqual").html("");
                    $("#requiredexp").html("");
                    var qualification = '';
                    var requiredexp = "";
                    if (json.orgDetails !== '') {
                        $("#en_collegelogo").prop('src', '<?php echo base_url(); ?>' + json.orgDetails[0].orgLogo);
                        $("#en_collegename").text(json.orgDetails[0].orgName);
                        $("#en_collegeaddress").text(json.orgDetails[0].orgAddress);
                        $("#approvedById").text((json.orgDetails[0].approvedBy !== "" ? "Approved By " + json.orgDetails[0].approvedBy : ''));
                    }
                    if (json.reqEligibility) {
                        if (json.reqEligibility.experiencetable) {
                            $("#expprereqDiv").html(json.reqEligibility.experiencetable);
                        }
                        if (json.reqEligibility.Minqualification !== "") {
                            qualification = json.reqEligibility.Minqualification;
                        }
                        if (json.reqEligibility.isElligible === "Yes") {
                            $("#iseligibleexp").removeClass("hidden");
                            $("#isnoteligibleexp").addClass("hidden");
                            $("#experienceDiv").addClass("greenborder");
                            $("#experienceDiv").removeClass("redborder");

                        } else {
                            $("#iseligibleexp").addClass("hidden");
                            $("#isnoteligibleexp").removeClass("hidden");
                            $("#experienceDiv").addClass("redborder");
                            $("#experienceDiv").removeClass("greenborder");
                        }
                        if (json.reqEligibility.Qualified === 'Yes') {
                            $("#iseligible").removeClass("hidden");
                            $("#isnoteligible").addClass("hidden");
                            $("#valEligibilityId").addClass("greenborder");
                            $("#valEligibilityId").removeClass("redborder");
                        } else if (json.reqEligibility.Qualified === 'No') {
                            $("#iseligible").addClass("hidden");
                            $("#isnoteligible").removeClass("hidden");
                            $("#valEligibilityId").addClass("redborder");
                            $("#valEligibilityId").removeClass("greenborder");
                        }
                        if (json.reqEligibility.Qualified === 'Yes' && json.reqEligibility.isElligible === "Yes" && json.isAgeEligible === "Yes") {
                            $("#submitbtn").html('<input type="submit" style="width: fit-content;    padding: 10px;  letter-spacing: 1px;" id="singlebutton" name="singlebutton" class="btn btn-success" value="Enroll Now">');
                        } else {
                            $("#submitbtn").html('<h5 class="bg-red-gradient" id="noteligibletext" style="padding:5px;">Sorry you are not eligible for this course.</h5>');
                        }
                    } else {
                        $("#singlebutton").removeClass('hidden');
                        $("#noteligibletext").remove();
                    }
                    $("#requiredqual").append(qualification);
                    $("#requiredexp").append(requiredexp);
                    $("#enrollModal").show();
                    $("body").css("overflow", "hidden");
                },
                error: function (response) {
                    $('#error').html(response);
                }
            });
        }
    // }
	}
    $("#closeenrollModal").click(function () {
        $("#enrollModal").hide();
        $("body").css("overflow", "auto");
    });
    $("#closeenqiryModal").click(function () {
        $("#enquiryModal").hide();
        $("body").css("overflow", "auto");
    });
</script>
<script>
    function findRelatedOrg() {
        var loginIds = $("#mainSearchbox").attr('loginIds');
        var searchTerm = $("#mainSearchbox").val();
        var roleName = $("#mainSearchbox").attr('orgType');
        var courseId = $("#mainSearchbox").attr('courseId');
        var url = '<?php echo site_url('allOrganizationDetails?'); ?>Type=' + roleName + '&searchterm=' + searchTerm + '&id=' + loginIds + '&courseId=' + courseId;
        window.location = url;
    }
    function addActiveHm(focussed) {
        currentFocusH = focussed;
        $(".searchterms").removeClass("autocomplete-active");
        $("#searchterm" + focussed).addClass("autocomplete-active");
    }
    function addActiveH(focussed) {
        var searchTh = $(".searchterms").height();
        var divheight = $("#searchboxautocomplete-list").height();
        var scrollh = $("#searchboxautocomplete-list").prop('scrollHeight');

        if (divheight < (focussed + 2) * (searchTh + 21) < scrollh) {
            $("#searchboxautocomplete-list").scrollTop((focussed + 1) * (searchTh + 21) - divheight);
        } else {
            $("#searchboxautocomplete-list").scrollTop(0);
        }
        $(".searchterms").removeClass("autocomplete-active");
        $("#searchterm" + focussed).addClass("autocomplete-active");
    }
    var currentFocusH = -1;
    function selElementH(focussed) {
        $("#mainSearchbox").val($("#searchterm" + focussed).text());
        $("#mainSearchbox").attr('loginIds', $("#searchterm" + focussed).attr('loginids'));
        $("#mainSearchbox").attr('orgType', $("#searchterm" + focussed).attr('roleName'));
        $("#mainSearchbox").attr('courseId', $("#searchterm" + focussed).attr('courseId'));
        $("#searchboxautocomplete-list").remove();
    }
    $(document).ready(function () {
        $("#singlebutton").click(function () {
            var options = {
                beforeSend: function () {
                },
                success: function (response) {
                    //console.log(response);
                    var json = $.parseJSON(response);
                    if (json.status === 'success') {
                        $.alert({
                            title: 'Success!', content: json.msg, type: 'blue',
                            typeAnimated: true,
                            buttons: {
                                Ok: function () {
                                    // window.location.reload();
                                }
                            }
                        });
                    } else {
                        $.alert({
                            title: 'Error!', content: json.msg, type: 'red',
                            typeAnimated: true,
                            buttons: {
                                Ok: function () {
                                    //window.location.reload();
                                }
                            }
                        });
                    }
                },
                error: function (jqXHR, exception) {
                    $.alert({
                        title: 'Error!', content: jqXHR["status"] + " - " + exception, type: 'red',
                        typeAnimated: true,
                        buttons: {
                            Ok: function () {

                            }
                        }
                    });
                }
            };
            $('#enrollNowForm').ajaxForm(options);
        });
        $("#enquiryBtn").click(function () {

            var options = {
                beforeSend: function () {
                },
                success: function (response) {
                    //console.log(response);
                    var json = $.parseJSON(response);
                    if (json.status === 'success') {
                        $.alert({
                            title: 'Success!', content: json.msg, type: 'blue',
                            typeAnimated: true,
                            buttons: {
                                Ok: function () {
                                    window.location.reload();
                                }
                            }
                        });
                    } else {
                        $.alert({
                            title: 'Error!', content: json.msg, type: 'red',
                            typeAnimated: true,
                            buttons: {
                                Ok: function () {
                                    window.location.reload();
                                }
                            }
                        });
                    }
                },
                error: function (jqXHR, exception) {
                    $.alert({
                        title: 'Error!', content: jqXHR["status"] + " - " + exception, type: 'red',
                        typeAnimated: true,
                        buttons: {
                            Ok: function () {

                            }
                        }
                    });
                }
            };
            $('#enquiryForm').ajaxForm(options);
        });
        $.ajax({
            type: "POST",
            url: "<?php echo site_url("home/siteVisitor"); ?>"
        });

        currentFocusH = -1;
        $("#mainSearchbox").click(function () {
            currentFocusH = -1;
        });
        $("#mainSearchbox").keyup(function (e) {

            if ($('#searchboxautocomplete-list').length) {
                if (e.keyCode === 40) {
                    /*If the arrow DOWN key is pressed,
                     increase the currentFocusH variable:*/
                    currentFocusH++;
                    /*and and make the current item more visible:*/
                    addActiveH(currentFocusH);
                    return true;
                } else if (e.keyCode === 38) { //up
                    /*If the arrow UP key is pressed,
                     decrease the currentFocusH variable:*/
                    currentFocusH--;
                    /*and and make the current item more visible:*/
                    addActiveH(currentFocusH);
                    return true;
                } else if (e.keyCode === 13) {
                    /*If the ENTER key is pressed, prevent the form from being submitted,*/
                    e.preventDefault();
                    if (currentFocusH > -1) {
                        /*and simulate a click on the "active" item:*/
                        selElementH(currentFocusH);
                        return true;
                    }
                } else {
                    currentFocusH = -1;
                }
            }
            if (e.keyCode >= 65 && e.keyCode <= 90) {
                var keySearch = $(this).val();
                $("#searchboxautocomplete-list").remove();
                $.ajax({
                    url: "<?php echo site_url("home/mainheaderFilter"); ?>",
                    type: 'POST',
                    data: {keySearch: keySearch},
                    success: function (response) {
                        if (response !== '') {
                            var responsearr = $.parseJSON(response);
                            var dropdowndata = '';
                            for (var i = 0; i < responsearr.length; i++) {
                                dropdowndata = dropdowndata + '<div id="searchterm' + i + '" onclick="selElementH(' + i + ');" onmouseover="addActiveHm(' + i + ');" loginids="' + responsearr[i].loginids + '" roleName="' + responsearr[i].roleName + '" courseId="' + responsearr[i].courseId + '" class="searchterms">' + responsearr[i].searchterm + '</div>';
                            }
                        } else {
                            var dropdowndata = '<div id="searchterm0" onclick="selElementH(0);" onmouseover="addActiveHm(0);" loginids="" class="searchterms">No Results Found</div>';
                        }
                        var divdata = '<div id="searchboxautocomplete-list" class="autocomplete-items">' + dropdowndata + '</div>';
                        $("#searchboxautocomplete-list").html("");
                        $("#mainSearchboxdiv").append(divdata);
                    }, error: function (response) {
                        $.alert({title: 'Error!', content: response, type: 'red',
                            typeAnimated: true, buttons: {
                                Ok: function () {
                                    window.location.reload();
                                }
                            }});
                    }
                });
            } else {
                return false;
            }
        });
        menuCourses();
        menuInstituteCourses();
        "use strict";

        $('.menu > ul > li:has( > ul)').addClass('menu-dropdown-icon');
        //Checks if li has sub (ul) and adds class for toggle icon - just an UI


        $('.menu > ul > li > ul:not(:has(ul))').addClass('normal-sub');
        //Checks if drodown menu's li elements have anothere level (ul), if not the dropdown is shown as regular dropdown, not a mega menu (thanks Luka Kladaric)

        $(".menu > ul").before("<a href=\"#\" class=\"menu-mobile\">Navigation</a>");

        //Adds menu-mobile class (for mobile toggle menu) before the normal menu
        //Mobile menu is hidden if width is more then 959px, but normal menu is displayed
        //Normal menu is hidden if width is below 959px, and jquery adds mobile menu
        //Done this way so it can be used with wordpress without any trouble

        $(".menu > ul > li").hover(function (e) {
            if ($(window).width() > 943) {
                $(this).children("ul").stop(true, false).fadeToggle(150);
                e.preventDefault();
            }
        });
        //If width is more than 943px dropdowns are displayed on hover

        $(".menu > ul > li").click(function (e) {
            if ($(window).width() <= 943) {
                $(this).children("ul").fadeToggle(150);
                e.preventDefault();
            }
        });
        //If width is less or equal to 943px dropdowns are displayed on click (thanks Aman Jain from stackoverflow)

        $(".menu-mobile").click(function (e) {
            $(".menu > ul").toggleClass('show-on-mobile');
            e.preventDefault();
        });
        //when clicked on mobile-menu, normal menu is shown as a list, classic rwd menu story (thanks mwl from stackoverflow)

    });
    function menuCourses() {
        $.ajax({
            url: "<?php echo site_url('Home/allCoursesDetails'); ?>",
            type: 'POST',
            data: '',
            success: function (response) {
                if (response) {
                    var json = $.parseJSON(response);
                    var data = '<li><a href="#">Course Type</a><ul>';
                    var data1 = '<li><a href="#">Course Type</a><ul>';
                    var x = 0;
                    for (var i = 0; i < json.length; i++) {
                        if (x !== 0 && x % 5 === 0) {
                            data = data + '</ul></li><li><a href="#">&nbsp;</a><ul>';
                            data1 = data1 + '</ul></li><li><a href="#">&nbsp;</a><ul>';
                        }
                        data = data + '<li><a href="<?php echo site_url("allOrganizationDetails?Type=University&CourseType="); ?>' + json[i].courseType + '">' + json[i].courseType + '</a></li>';
                        data1 = data1 + '<li><a href="<?php echo site_url("allOrganizationDetails?Type=College&CourseType="); ?>' + json[i].courseType + '">' + json[i].courseType + '</a></li>';
                        x++;
                    }
                    if (x !== 0 && x % 5 !== 0) {
                        data = data + '</ul> </li></li>';
                        data1 = data1 + '</ul> </li></li>';
                    }
                    $("#universityDropDown").append(data);
                    $("#collegeDropDown").append(data1);
                    // console.log(data);
                }
            }
        });
    }
    $('#registerStudent').click(function () {
        var options = {
            beforeSend: function () {
            },
            success: function (response) {
                console.log(response);
                var json = $.parseJSON(response);
                if (json.status === 'success') {
                    $.alert({title: 'Success!', content: json.msg, type: 'blue',
                        typeAnimated: true,
                        buttons: {
                            Ok: function () {
                                window.location.reload();
                            }
                        }
                    });
                } else {
                    $.alert({title: 'Error!', content: json.msg, type: 'red',
                        typeAnimated: true, buttons: {
                            Ok: function () {

                            }
                        }
                    });
                }
            },
            error: function (jqXHR, exception) {
                $.alert({
                    title: 'Error!', content: jqXHR["status"] + " " + exception, type: 'red',
                    typeAnimated: true,
                    buttons: {
                        Ok: function () {
                            window.location.reload();
                        }
                    }
                });
            }
        };
        $('#studentRegistrationForm').ajaxForm(options);
    });

    function menuInstituteCourses() {
        $.ajax({
            url: "<?php echo site_url('Home/allInstCourses'); ?>",
            type: 'POST',
            data: '',
            success: function (response) {
                if (response) {
                    var json = $.parseJSON(response);
                    var data = '<li><a href="#">Course Type</a><ul>';
                    var x = 0;
                    for (var i = 0; i < json.length; i++) {
                        if (x !== 0 && x % 5 === 0) {
                            data = data + '</ul></li><li><a href="#">&nbsp;</a><ul>';
                        }
                        data = data + '<li><a href="<?php echo site_url("allOrganizationDetails?Type=Institute&CourseType="); ?>' + json[i].title + '">' + json[i].title + '</a></li>';
                        x++;
                    }
                    if (x !== 0 && x % 5 !== 0) {
                        data = data + '</ul> </li></li>';
                    }

                    $("#instituteDropDown").append(data);
                    // console.log(data);
                }
            }
        });
    }


    function getCountries(countryId, db_countryId, stateID) {
        $.ajax({
            url: "<?php echo site_url('Home/getCountriesJson'); ?>",
            type: 'POST',
            data: '',
            success: function (response) {
                var cid = '';
                var json = $.parseJSON(response);
                var data = '<option value="">Country</option>';
                for (var i = 0; i < json.length; i++) {
                    if (db_countryId === json[i].countryId) {
                        cid = 'selected';
                        var selected_stateId = "";
                        getcountryByStates(json[i].countryId, selected_stateId, stateID);
                    } else {
                        cid = '';
                    }
                    data = data + '<option ' + cid + '  value="' + json[i].countryId + '">' + json[i].name + ' (' + json[i].sortname + ')' + '</option>';
                }

                $(countryId).html(data);
            }
        });
    }
    function getcountryByStates(countryId, selected_stateId, stateId) {
        $.ajax({
            url: "<?php echo site_url('Home/getStatesByCountry'); ?>",
            type: 'POST',
            data: 'countryId=' + countryId,
            success: function (response) {
                var json = $.parseJSON(response);
                var data = '<option value="">State</option>';
                for (var i = 0; i < json.length; i++) {
                    data = data + '<option value="' + json[i].stateId + '">' + json[i].name + '</option>';
                }
                $(stateId).html(data);
                $(stateId).val(selected_stateId);
            }
        });
    }
    function getStateByCity(stateId, selected_cityId, cityId) {
        $.ajax({
            url: "<?php echo site_url('Home/getCityByStates') ?>",
            type: 'POST',
            data: 'stateId=' + stateId,
            success: function (response) {
                var json = $.parseJSON(response);
                var data = '<option value="">City</option>';
                for (var i = 0; i < json.length; i++) {
                    data = data + '<option value="' + json[i].cityId + '">' + json[i].name + '</option>';
                }
                $(cityId).html(data);
                $(cityId).val(selected_cityId);
            }
        });
    }
    $(document).ready(function () {
        getCountries('#countryId1', '<?php
if (isset($_SESSION['countryId'])) {
    echo $_SESSION['countryId'];
}
?>', '#stateId1');

        var db_countryId = '<?php
if (isset($_SESSION['countryId'])) {
    echo $_SESSION['countryId'];
}
?>';
        $.validate({
            lang: 'en'
        });
        getCountries('#countryId', db_countryId, '#stateId');

        $('#countryId').change(function () {
            var countryId = $('#countryId').val();

            var selected_stateId = "";
            getcountryByStates(countryId, selected_stateId, '#stateId');
        });
        $('#countryId1').change(function () {
            var countryId = $('#countryId1').val();

            var selected_stateId = "";
            getcountryByStates(countryId, selected_stateId, '#stateId1');
        });
        $('#stateId').change(function () {
            var stateId = $('#stateId').val();
            var selected_cityId = "";
            getStateByCity(stateId, selected_cityId, '#cityId');
        });

        $('#stateId1').change(function () {
            var stateId = $('#stateId1').val();
            var selected_cityId = "";
            getStateByCity(stateId, selected_cityId, '#cityId1');
        });


        $('#save_user').click(function () {
            var options = {
                beforeSend: function () {
                },
                success: function (response) {
                    if(response){
						let json = $.parseJSON(response);
						if (json.status == 'success')
						{
							alert(json.msg);
							$('#myModal').hide();

						} else {
							$('#message').text(json.msg);

						}
					}
                },
                error: function (response) {
                    $('#error').html(response);
                }
            };
            $('#register_form').ajaxForm(options);
        });
        $('.city-name').click(function () {
            var id = $(this).attr("id");
            $.ajax({
                type: "POST",
                data: "id=" + id,
                url: "<?php echo site_url('Home/countryValue'); ?>",
                success: function (response) {
                    location.reload();

                }
            });
        });
        $('.region-list').click(function () {
            var id = $(this).attr("id");
            $.ajax({
                type: "POST",
                data: "id=" + id,
                url: "<?php echo site_url('Home/countryValue'); ?>",
                success: function (response) {
                    location.reload();
                }
            });
        });

        $('#studentlogin').click(function () {
            var options = {
                beforeSend: function () {
                },
                success: function (response) {
                    var json = $.parseJSON(response);
                    if (json.status === 'success') {
                        window.location.reload();
                    } else {
                        $("#messagesdiv").html('<a href="#" style="float:right;"   data-dismiss="alert" id="closeme" aria-label="close">&times;</a>' + json.msg);
                        $("#messagesdiv").removeClass("hidden");

                    }
                },
                error: function (response) {
                    $('#error').html(response);
                }
            };
            $('#studentLoginForm').ajaxForm(options);
        });
        $("#closeme").click(function () {
            window.location.reload();
        });
        $("#countryId").change(function () {
            var countryId = $(this).val();
            $.ajax({
                url: '<?php echo site_url('Home/setCountry'); ?>',
                data: {countryId: countryId},
                type: "POST",
                success: function (response) {
                    var json = $.parseJSON(response);
                    if (json.status === "success") {

                        $.alert({title: 'Success!', content: json.msg, type: 'blue',
                            typeAnimated: true,
                            buttons: {
                                Ok: function () {
                                     window.location.href = '<?php echo site_url(); ?>';
                                }
                            }
                        });
                    } else {
                        $.alert({
                            title: 'Error!', content: json.msg, type: 'red',
                            typeAnimated: true,
                            buttons: {
                                Ok: function () {
                                    window.location.href = '<?php echo site_url(); ?>';
                                }
                            }
                        });
                    }
                },
                error: function (jqXHR, exception) {
                    $.alert({
                        title: 'Error!', content: jqXHR["status"] + " " + exception, type: 'red',
                        typeAnimated: true,
                        buttons: {
                            Ok: function () {
                                window.location.href = '<?php echo site_url(); ?>';
                            }
                        }
                    });
                }
            });
        });
    });

</script>
<script>
    $(window).load(function (e) {
        $('.hunt-modal').show();

    });
<?php if (!isset($_SESSION['countryId'])) { ?>
        if ($('#hunt-modal').show()) {
            $('body').addClass('_fixed');
        }
    <?php
} else {
    echo "$('body').removeClass('_fixed')";
}
?>

</script>

<!-----------------modal_--------------------------->


<script>
    var studentLoginCaptcha;
    var onloadCallback = function () {

        widgetId1 = grecaptcha.render('studentLoginCaptcha', {
            'sitekey': '6LfYFV0UAAAAAPmeRqPDdUCFNWvDbR3IXgyKMNp3',
            'theme': 'dark'
        });

    };
    $('.loading').fadeOut('slow');
    $(document).ready(function () {
        $('.loading').fadeOut('slow');
        $("#myBtn").click(function () {
            $("#myModal").modal('show');
        });
        $(".close").click(function () {
            $("#myModal").modal('hide');
        });
    });

</script>


<script>
    function openCity(cityName, elmnt, color) {
        var i, tabcontent, tablinks;
        tabcontent = document.getElementsByClassName("tabcontent");
        for (i = 0; i < tabcontent.length; i++) {
            tabcontent[i].style.display = "none";
        }
        tablinks = document.getElementsByClassName("tablink");
        for (i = 0; i < tablinks.length; i++) {
            tablinks[i].style.backgroundColor = "";
        }
        document.getElementById(cityName).style.display = "block";
        elmnt.style.backgroundColor = color;

    }

    // Get the element with id="defaultOpen" and click on it
    document.getElementById("defaultOpen").click();
    $(document).ready(function () {
        $('.numOnly').keydown(function (e) {
            if (e.shiftKey || e.ctrlKey || e.altKey) {
                e.preventDefault();
            } else {
                var key = e.keyCode;
                if (!((key == 8) || (key == 9) || (key == 46) || (key >= 35 && key <= 40) || (key >= 48 && key <= 57) || (key >= 96 && key <= 105))) {
                    //if (key > 31 && (key < 48 || key > 57))

                    e.preventDefault();
                }
            }
        });
    });
    function signOut() {
        var auth2 = gapi.auth2.getAuthInstance();
        auth2.signOut().then(function () {
            console.log('User signed out.');
        });
    }
<?php echo (isset($_SESSION['studentId']) ? '' : 'signOut();'); ?>
    function onSignIn(googleUser) {
        var profile = googleUser.getBasicProfile();
        var signedin = '<?php echo (isset($_SESSION['studentId']) ? "Yes" : 'No'); ?>';
        if (profile.getName() !== "" && profile.getEmail() !== "" && signedin === 'No') {
            $.ajax({
                url: '<?php echo site_url('Register/loginWithGoogle'); ?>',
                data: {name: profile.getName(), email: profile.getEmail(), image: profile.getImageUrl()},
                type: "POST",
                success: function (response) {
                    var json = $.parseJSON(response);
                    if (json.status === "success") {

                        $.alert({title: 'Success!', content: json.msg, type: 'blue',
                            typeAnimated: true,
                            buttons: {
                                Ok: function () {
                                    window.location.href = '<?php echo site_url(); ?>'
                                }
                            }
                        });
                    } else {
                        $.alert({
                            title: 'Error!', content: json.msg, type: 'red',
                            typeAnimated: true,
                            buttons: {
                                Ok: function () {
                                    window.location.href = '<?php echo site_url(); ?>'
                                }
                            }
                        });
                    }
                },
                error: function (jqXHR, exception) {
                    $.alert({
                        title: 'Error!', content: jqXHR["status"] + " " + exception, type: 'red',
                        typeAnimated: true,
                        buttons: {
                            Ok: function () {
                                window.location.href = '<?php echo site_url(); ?>';
                            }
                        }
                    });
                }
            });
        }
    }
    function renderButton() {
        gapi.signin2.render('my-signin2', {
            'scope': 'profile email',
            'width': 240,
            'height': 50,
            'longtitle': true,
            'theme': 'dark',
            'onsuccess': onSignIn,
            'onfailure': onFailure
        });
    }
    function onFailure() {
        $.alert({
            title: 'Error!', content: "Google sign in failure", type: 'red',
            typeAnimated: true,
            buttons: {
                Ok: function () {
                    window.location.href = '<?php echo site_url(); ?>';
                }
            }
        });
    }
</script>
<script src="https://apis.google.com/js/platform.js?onload=renderButton" async defer></script>

<script>

	//////////////////////////////////////////////////////////////////////////////////
	
	
	 function statusChangeCallback(response) {  // Called with the results from FB.getLoginStatus().
    console.log('statusChangeCallback');
    console.log(response);                   // The current login status of the person.
    if (response.status === 'connected') {   // Logged into your webpage and Facebook.
      testAPI();  
    } else {                                 // Not logged into your webpage or we are unable to tell.
      document.getElementById('status').innerHTML = 'Please log ' +
        'into this webpage.';
    }
  }


  function checkLoginState() {               // Called when a person is finished with the Login Button.
    FB.getLoginStatus(function(response) {   // See the onlogin handler
      statusChangeCallback(response);
    });
  }


  window.fbAsyncInit = function() {
    FB.init({
      appId      : '{294729754539736}',
      cookie     : true,                     // Enable cookies to allow the server to access the session.
      xfbml      : true,                     // Parse social plugins on this webpage.
      version    : 'v3.2'           // Use this Graph API version for this call.
    });


    FB.getLoginStatus(function(response) {   // Called after the JS SDK has been initialized.
      statusChangeCallback(response);        // Returns the login status.
    });
  };

  
  (function(d, s, id) {                      // Load the SDK asynchronously
    var js, fjs = d.getElementsByTagName(s)[0];
    if (d.getElementById(id)) return;
    js = d.createElement(s); js.id = id;
    js.src = "https://connect.facebook.net/en_US/sdk.js";
    fjs.parentNode.insertBefore(js, fjs);
  }(document, 'script', 'facebook-jssdk'));

    function signOut() {
       FB.logout(function(response) {
         console.log('Person is now logged out');
       });
    }
	
<?php echo (isset($_SESSION['studentId']) ? '' : 'signOut();'); ?>
  function testAPI() {                      // Testing Graph API after login.  See statusChangeCallback() for when this call is made.
    console.log('Welcome!  Fetching your information.... ');
    FB.api('/me',{fields: "id,email,gender,name,last_name"}, function(response) {
	   var signedin = '<?php echo (isset($_SESSION['studentId']) ? "Yes" : "No"); ?>';
        if (response.name !== "" && response.email !== "" && signedin === 'No') {
            $.ajax({
                url: '<?php echo site_url('Register/loginWithFaceBook'); ?>',
                data: {name: response.name, email: response.email},
                type: "POST",
                success: function (response) {
                    var json = $.parseJSON(response);
                    if (json.status === "success") {

                        $.alert({title: 'Success!', content: json.msg, type: 'blue',
                            typeAnimated: true,
                            buttons: {
                                Ok: function () {
                                    window.location.href = '<?php echo site_url(); ?>';
                                }
                            }
                        });
                    } else {
                        $.alert({
                            title: 'Error!', content: json.msg, type: 'red',
                            typeAnimated: true,
                            buttons: {
                                Ok: function () {
                                    window.location.href = '<?php echo site_url(); ?>';
                                }
                            }
                        });
                    }
                },
                error: function (jqXHR, exception) {
                    $.alert({
                        title: 'Error!', content: jqXHR["status"] + " " + exception, type: 'red',
                        typeAnimated: true,
                        buttons: {
                            Ok: function () {
                                window.location.href = '<?php echo site_url(); ?>';
                            }
                        }
                    });
                }
            });
        }
      //document.getElementById('status').innerHTML =
        'Thanks for logging in, ' + response.name + '!';
    });
  }


</script>
<!--------------------modal-------------------------->

</body>

</html>
